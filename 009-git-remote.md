### Push local repository to github
first, create a repository in github

### change local branch name from master to main
```bash
$ git branch -M main
```
### add origin
```bash
$ git remote add origin https://github.com/brandon-kingsinfo/learning-git.git
$ git push -u origin main
```
