### push local repo to github
1. create a repo in github
```
### change local branch name from master to main
$ git branch -M main
### add origin
$ git remote add origin  origin https://github.com/brandon-kingsinfo/learning-git.git
$ git push -u origin main

